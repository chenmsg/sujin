@model respLendTargetFeeQueryModel
@{
    ViewBag.Title = "结算手续费查询接口";
}


<form>

    <div class="form-group">
        <label for="exampleInputEmail1">用户ID</label>
        <input type="text" class="form-control" name="UserId" value="@TQuery.GetSafeString("UserId")" placeholder="用户ID">
    </div>
    <div class="form-group">
        <label for="exampleInputEmail1">结算金额</label>
        <input type="text" class="form-control" name="Amount" value="@TQuery.GetSafeString("Amount")" placeholder="结算金额">
    </div>
    <button type="submit" class="btn btn-default">提交</button>
</form>
<div class="alert alert-block alert-info fade in">
    <button data-dismiss="alert" class="close" type="button">×</button>
    <h4 class="alert-heading">查询消息:</h4>
    <p>@(Model.message)</p>

</div>


@if (Model.backState == 0)
{


        <div class="alert alert-block alert-info fade in">
            <button data-dismiss="alert" class="close" type="button">×</button>
            <h4 class="alert-heading">查询信息：</h4>
            <p>结算方式:@(Model.transType)</p>
            <p>结算金额:@(Model.transAmount)</p>
            <p>查询时间:@(Model.queryTime)</p>
            <p>手续费:@(Model.targetFee)</p>
        </div>

}

<div class="alert alert-block alert-error fade in">
    <button data-dismiss="alert" class="close" type="button">×</button>
    <h4 class="alert-heading">返回报文</h4>
    <p>@(JsonConvert.SerializeObject(Model))</p>

</div>




